import os, requests
from bs4 import BeautifulSoup

TOKEN = os.getenv('TG_TOKEN')
CHAT_ID = os.getenv('TG_CHAT_ID')

def get_matches():
    url = "https://scoreyab.com/"
    headers = {
        'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/119.0.0.0 Safari/537.36'
    }
    
    try:
        response = requests.get(url, headers=headers, timeout=20)
        response.encoding = 'utf-8'  # ??????? ?? ????? ???? ?????
        soup = BeautifulSoup(response.text, 'html.parser')
        
        # ???? ???? ???? ???? ??????? (???????? ??????? ?? ????????? ?????)
        # ?????? ?? ???????? ?????? ??????? ?? ??????? ?? ????????? ??? 'match-item' ?? 'match-wrapper' ?????
        match_containers = soup.select('.match-item, .match-wrapper, .match-row')
        
        if not match_containers:
            # ??? ??? ???? ???? ????? ?? ???? ?? ?? ??? ??? ?????? ?? ?????? ?????? ????
            print("DEBUG: Could not find matches. Page snippet:")
            print(soup.prettify()[:1000]) 
            return "? ????????? ???? ??? (?????? ???? ????? ???? ???)."
        
        matches = [f"?? {m.get_text(strip=True)}" for m in match_containers][:10]
        return "?? ?????? ???????? ?????:\n\n" + "\n".join(matches)

    except Exception as e:
        return f"? ???? ?????: {str(e)}"

def send_telegram(text):
    if not TOKEN or not CHAT_ID: return
    url = f"https://api.telegram.org/bot{TOKEN}/sendMessage"
    payload = {"chat_id": CHAT_ID, "text": text, "parse_mode": "HTML"}
    requests.post(url, json=payload)

if __name__ == "__main__":
    msg = get_matches()
    send_telegram(msg)
